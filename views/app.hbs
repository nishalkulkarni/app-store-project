<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Devices - Software Store</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.min.js"
        crossorigin="anonymous"></script>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">SoftStore</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="devices">Devices</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="rankings">Rankings</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="logout">Log Out</a>
                </li>
            </ul>
        </div>
    </nav>

    {{#if message }}
    <h4 class="alert alert-danger mt-4">{{message}}</h4>
    {{/if}}

    <div class="container" style="margin-top: 2em; min-width: 75%;" id="mainContainer">

        {{#each results}}
        <div class="card mt-4">
            <div class="card-body">
                <h2 class="card-title">{{this.app_name}} [{{this.os}}]</h2>
                <p class="card-text">Description: {{this.description}}</p>
                <p class="card-text">Developer: {{this.dev_name}}</p>
                <p class="card-text">Developer Email: {{this.dev_email}}</p>

                <p class="card-text">Date of publishing: {{this.date_of_publishing}}</p>
                <p class="card-text">Last Updated: {{this.last_updated}}</p>

                <p class="card-text">Cost: {{this.cost}}</p>
                <p class="card-text">Payment Link:
                    <a href="{{this.payment_link}}">Pay</a>
                </p>

                <p class="card-text">Version 1:
                    <a href="{{this.version_1_link}}">Install</a>
                </p>
                <p class="card-text">Version 2:
                    <a href="{{this.version_2_link}}">Install</a>
                </p>
                <p class="card-text">Version 3:
                    <a href="{{this.version_3_link}}">Install</a>
                </p>
            </div>
        </div>

        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title">Give your feedback</h4>
                <form action="/app/{{this.app_id}}" method="POST">
                    <div class="form-group">
                        <label for="rating">Rating</label>
                        <input type="number" min="1" max="5" value="3" class="form-control" id="rating" name="rating">
                    </div>
                    <div class="form-group">
                        <label for="review">Review</label>
                        <input type="text" class="form-control" id="review" name="review">
                    </div>
                    <button type="submit" class="btn btn-primary">Submit feedback</button>
                </form>
            </div>
        </div>

        {{/each}}




    </div>

</body>

</html>