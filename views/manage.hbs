<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Apps - Software Store</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.min.js"
        crossorigin="anonymous"></script>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">SoftStore</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/manage">Manage Apps <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/devices">Devices/Apps</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/rankings">Rankings</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Log Out</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container" style="margin-top: 2em; min-width: 75%;" id="mainContainer">
        <div class="card mt-4">
            <div class="card-header">
                <strong>Manage your apps</strong>
            </div>
            <div class="card-body">
                <form action="/manage" method="POST">
                    <div class="form-group">
                        <label for="userApps">Select an app: </label>
                        <select name="userApps" id="userApps">
                            {{#each aps}}
                            <option value="{{this.app_id}}">{{this.app_name}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="userDevices">Select device: </label>
                        <select name="userDevices" id="userDevices">
                            {{#each devcs}}
                            <option value="{{this.device_id}}">{{this.os}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="userAction">Select action: </label>
                        <select name="userAction" id="userAction">
                            <option value="useVersion1">Use Version 1</option>
                            <option value="useVersion2">Use Version 2</option>
                            <option value="useVersion3">Use Version 3</option>
                            <option value="uninstall">Uninstall</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-warning">Perform Action</button>
                </form>
            </div>
        </div>

        {{#if message }}
        <h4 class="alert alert-danger mt-4">{{message}}</h4>
        {{/if}}

    </div>

</body>

</html>